# Ensure REACT_APP_API_URL matches the backend port (default: 5000)
# If backend fails to start on port 5000, update this URL accordingly
REACT_APP_API_URL=http://localhost:5000/api
REACT_APP_NAME=StudyHub